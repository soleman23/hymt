{
  "name": "hymt-image-pipeline",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Place-card image generation pipeline. Deliberately separate from the site's package.json so Playwright never enters the Astro dependency tree.",
  "scripts": {
    "test": "node test/tracker.test.mjs && node test/model-router.test.mjs && node test/runlog.test.mjs",
    "dry-run": "node run.mjs --dry-run",
    "login": "node run.mjs --login",
    "probe": "node run.mjs --probe",
    "pilot": "node run.mjs --limit 3"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
